# Short Lambda Project

A tiny AWS Lambda example in Python.

## Files
- `lambda_function.py` – Lambda handler (`lambda_handler`).
- `test_lambda_function.py` – basic unit tests.

## Run locally
```bash
python -m unittest -v
```

## Example event
```json
{
  "name": "Lmbda"
}
```
